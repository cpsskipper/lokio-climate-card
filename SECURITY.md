# Security policy

Please do not publish Home Assistant access tokens, passwords, `secrets.yaml`, private URLs, or other credentials in an issue.

For ordinary bugs, open a GitHub issue with a minimal configuration using anonymized entity IDs. For a security-sensitive report, contact the repository owner privately through the contact method listed on their GitHub profile until a dedicated security-reporting channel is configured.
